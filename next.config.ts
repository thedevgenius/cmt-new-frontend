import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['10.142.128.149', 'localhost', '127.0.0.1', '10.74.154.149'],
};

export default nextConfig;
